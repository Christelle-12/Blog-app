<h1>Create Comment</h1>

<%= form_with(model: @comment, url: user_post_comments_path(params[:user_id], params[:post_id])) do |form| %>
  <%= form.text_area :text, placeholder: 'Your comment' %>
  <%= form.submit 'Create Comment' %>
<% end %>
